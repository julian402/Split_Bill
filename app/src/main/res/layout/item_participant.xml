<?xml version="1.0" encoding="utf-8"?>
<!--
    Participante de un gasto: casilla, avatar y nombre. En partes iguales van dos por fila; con montos
    exactos o porcentajes, uno por fila con su campo de valor.
-->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cardParticipant"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/colorStroke">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:minHeight="60dp"
        android:orientation="horizontal"
        android:paddingStart="6dp"
        android:paddingEnd="8dp">

        <!-- La casilla no recibe el toque: lo recibe toda la tarjeta -->
        <CheckBox
            android:id="@+id/cbParticipant"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:clickable="false"
            android:focusable="false"
            android:minWidth="0dp"
            android:minHeight="0dp" />

        <TextView
            android:id="@+id/tvParticipantAvatar"
            style="@style/Widget.SplitBill.Avatar"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:textSize="14sp"
            tools:text="A" />

        <TextView
            android:id="@+id/tvParticipantName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/colorTextPrimary"
            android:textSize="15sp"
            tools:text="Andres" />

        <EditText
            android:id="@+id/etParticipantValue"
            android:layout_width="112dp"
            android:layout_height="44dp"
            android:layout_marginStart="8dp"
            android:autofillHints="none"
            android:background="@drawable/bg_input"
            android:hint="@string/hintValueExact"
            android:inputType="numberDecimal"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:textAlignment="viewEnd"
            android:textColor="@color/colorTextPrimary"
            android:textColorHint="@color/colorTextSecondary"
            android:textSize="14sp"
            android:visibility="gone"
            tools:visibility="visible" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
