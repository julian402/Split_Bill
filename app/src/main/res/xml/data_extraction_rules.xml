<?xml version="1.0" encoding="utf-8"?>
<!--
    Respaldo y traspaso a otro celular (Android 12 en adelante): no se copia nada.

    Todo lo que guarda la app es una copia de lo que esta en el servidor (splitbill.db) o depende de
    este celular: el token esta cifrado con una llave del Android Keystore, que nunca sale del
    dispositivo, asi que restaurado en otro celular no se podria leer. Los datos vuelven solos al
    iniciar sesion y sincronizar.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
