<?xml version="1.0" encoding="utf-8"?>
<!-- Integrante del grupo: avatar, nombre, telefono, papel (Administrador / Integrante) y papelera -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginTop="6dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="6dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingTop="14dp"
        android:paddingEnd="14dp"
        android:paddingBottom="14dp">

        <TextView
            android:id="@+id/tvMemberInitials"
            style="@style/Widget.SplitBill.Avatar"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:textSize="20sp"
            tools:text="A" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvMemberNames"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textAppearance="@style/TextAppearance.SplitBill.ItemTitle"
                tools:text="Andres" />

            <TextView
                android:id="@+id/tvMemberPhone"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:maxLines="1"
                android:textAppearance="@style/TextAppearance.SplitBill.ItemSubtitle"
                tools:text="3111231234" />
        </LinearLayout>

        <TextView
            android:id="@+id/tvMemberRole"
            style="@style/Widget.SplitBill.Chip.Neutral"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            tools:text="Administrador" />

        <ImageButton
            android:id="@+id/btnDeleteMember"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginStart="10dp"
            android:background="@drawable/bg_delete_button"
            android:contentDescription="@string/cdDelete"
            android:src="@drawable/ic_delete"
            app:tint="@color/colorDebtor" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
