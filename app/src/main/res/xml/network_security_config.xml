<?xml version="1.0" encoding="utf-8"?>
<!--
    Android bloquea por defecto las conexiones HTTP sin cifrar. Aqui se permiten SOLO hacia el
    computador de desarrollo (10.0.2.2 es el PC visto desde el emulador), donde el backend corre sin
    HTTPS. Cualquier otro servidor, incluido el backend desplegado, sigue exigiendo HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <!-- servidor falso de las pruebas instrumentadas (MockWebServer) -->
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
